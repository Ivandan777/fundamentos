#include <iostream>
  
  using namespace std;
int main()
{
    float base,altura,area;
    cout<<"ingresa el valor de la altura";
    cin>>base;
    cout<<"ingresa el valor de la altura";
    cin>>altura;
    area=(base*altura)/2;
    cout<<"el area es:" <<area;
    return 0;
}
